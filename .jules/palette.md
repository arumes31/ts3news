## 2024-10-24 - Added Loading States to Inventory Buttons
**Learning:** Added visual loading states (`.btn-busy`) to inventory actions to prevent double submission and improve UX.
**Action:** Use `.btn-busy` class and `disabled` attribute on buttons during async operations.
